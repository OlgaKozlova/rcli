/* export default function (parameters, options, commandConfiguration, dataSource) {
    const setName = parameters.setName;
    const bundleConfiguration = dataSource[commandConfiguration.data][parameters[0]];

    bundleConfiguration.forEach(config => {
        // read template
        // compile it
    })
}; */
