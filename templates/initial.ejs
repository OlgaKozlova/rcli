<% var fields = fields || [] -%>
<% var buttons = buttons || [] -%>
import Immutable from 'immutable';

export const <%= featureName %>InitialState = new Immutable.Map({
<% fields.forEach(function(field){ -%>
    <%- field %>: '',
<% }); -%>
});
